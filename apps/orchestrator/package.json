{
  "name": "postiz-orchestrator",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "dotenv -e ../../.env -- nodemon --watch src --watch ../../libraries --ext ts --exec \"node -r @swc-node/register src/main.ts\"",
    "build": "cross-env NODE_ENV=production nest build -b swc",
    "start": "dotenv -e ../../.env -- node --experimental-require-module ./dist/apps/orchestrator/src/main.js",
    "pm2": "pm2 start pnpm --name orchestrator -- start"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
